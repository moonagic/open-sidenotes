# 下阶段实施任务清单

**日期：** 2026-03-30  
**状态：** 里程碑 2 进行中（P0 代码已落地，P1 部分任务代码已落地待验收）  
**负责人：** Codex

## 目标

围绕 notes-first 主线推进下阶段工作：
- 第一优先：提升记录/编辑速度与可靠性。
- 第二优先：简化笔记工作流中的导航。
- 第三优先：在不增加复杂度的前提下做精细化打磨。

## 范围约束

- 不重新引入“任务管理器”式信息架构。
- 不为高频操作增加重型多步骤流程。
- 设置入口必须保持在笔记工作区可快速触达。
- 每个迭代保持改动聚焦、可验证、可随时交付。

## 里程碑 1：保存链路可靠性（P0）

### 任务

- [x] 加固 front matter 读写，确保标题包含 `:` 或特殊字符时可正确往返。
- [x] 增加内存索引 `noteID -> fileURL`，避免保存/删除时反复全目录扫描。
- [x] 确保快速重命名、同名笔记场景下文件命名唯一性稳定。
- [x] 增加生命周期兜底：窗口隐藏/应用退出前强制落盘当前草稿。
- [x] 统一保存/加载错误展示，避免静默失败导致数据丢失。
- [x] 补齐存储边界场景测试。

### 验收标准

- [x] 连续多次重命名不会丢失笔记内容。
- [x] 标题含 `:` 的笔记可正确保存并重载。
- [x] 更新/删除不再依赖反复全目录扫描。
- [x] 隐藏/退出前最后一次编辑能持久化。
- [ ] `xcodebuild -project open-sidenotes.xcodeproj -scheme open-sidenotes -destination 'platform=macOS' test` 通过。

### 手工验证

- [ ] 新建笔记快速输入后立即隐藏窗口，重新打开后最后一行仍存在。
- [ ] 同一笔记连续重命名 10 次以上，仅保留预期文件。
- [ ] 创建两条同名笔记并重启应用，确认都可正确打开。
- [ ] 使用包含冒号和非 ASCII 字符的标题，重启后标题与内容正确。

## 里程碑 2：编辑吞吐 + 导航简化（P1）

### 任务

- [x] 增加保存去重：标题/内容归一化后无变化则跳过写盘。
- [x] 减少块编辑热点路径中的不必要解析/渲染。
- [x] 统一抽屉搜索与 Quick Open 的检索排序行为。
- [x] 提升大笔记集下抽屉搜索响应速度。
- [x] 确保 Quick Open/抽屉键盘交互行为稳定可预期。
- [x] 增加排序、去重、选择行为相关测试。

### 验收标准

- [ ] 快速输入时不再产生明显冗余写入。
- [ ] 抽屉与 Quick Open 对同一查询的前列结果趋于一致。
- [ ] 编辑过程中导航快捷键保持响应稳定。
- [ ] 现有 slash command 与代码块交互无回归。
- [ ] 主测试方案通过。

### 手工验证

- [ ] 连续输入 20 秒以上，编辑器无可感知卡顿。
- [ ] 抽屉与 Quick Open 输入同一关键词，对比前 5 结果。
- [ ] 反复使用 `⌘J`、上下箭头、回车、Esc，焦点行为稳定。
- [ ] 验证 `⌘N`、`⌘D`、`⌘,` 在编辑区仍可正常使用。

## 里程碑 3：打磨与可维护性收口（P2）

### 任务

- [ ] 审计并清理未使用/过时视图路径，或做必要合并。
- [ ] 规范编辑器与搜索相关组件的命名和职责边界。
- [ ] 补齐“切换笔记 + 自动保存先后顺序”关键测试空白。
- [ ] 同步计划/文档状态，确保文档与实际行为一致。
- [ ] 合并前完成最终回归检查清单。

### 验收标准

- [ ] 旧路径无残留死代码。
- [ ] 存储、编辑、导航模块边界更清晰。
- [ ] notes-first 核心路径具备回归测试覆盖。
- [ ] 文档与当前功能、快捷键保持一致。

### 手工验证

- [ ] 新建 -> 编辑 -> 切换 -> 重开路径行为正确。
- [ ] 删除当前选中笔记后能选择正确兜底笔记。
- [ ] 设置入口仍然保持一步可达。
- [ ] 小窗口下 header/editor/drawer/quick-open 可用性正常。

## 执行顺序

1. 里程碑 1（P0）：先打可靠性底座。
2. 里程碑 2（P1）：再做性能和导航体验。
3. 里程碑 3（P2）：最后收口与发布前打磨。

## 风险说明

- 最大风险在保存链路改动，需用“定向测试 + 手工验证”双保险。
- 搜索排序改动会直接影响体感，必须小步调参、逐步验证。
- 清理类改动应放在可靠性改造之后，避免混合风险。

## 汇报机制

每完成一个里程碑，统一按以下格式汇报：
1. 改动文件清单。
2. 测试命令与结果。
3. 手工验证清单完成情况。
4. 已知权衡与后续跟进项。
